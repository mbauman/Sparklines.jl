language: julia
julia:
  - 0.7
  - nightly
notifications:
  email: false
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.init(); Pkg.clone(pwd())'
#  - julia ./test/runtests.jl
